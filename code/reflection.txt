Write your reflection here.

See reflection.md for what makes a good reflection — be specific, use the
terminology from class, and make it actionable.

In this assignment I learned how a module like bill.py avoids the logic that is involved in a console, notebook, and streamlit
all of those import in logic while a module doesn't. Even though my overall program was pretty much the same for all four functions.
I learned how to use unit tests to check over my work along with sharpening my skills with the debugging tool. I learned about the 
differences between a unit test and an integration test as the integration test of test_grand_total went over the whole code of my 
module. When that failed I learned how to use assertion to check my work to as I no longer needed to guess and instead could see the
I needed to get for the code to be correct. I struggled with some of the code in the explore.ipynb module as I had some errors running
the notebook as in the second block and the 6th line was creating an error I found out that pandas was failing to install so after
restarting codespaces it worked great. 
